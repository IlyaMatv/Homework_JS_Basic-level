 var a = Number(prompt('Введите первое число'));
 var b = Number(prompt('Введите второе число'));
 var c = prompt('Введите + - * или /');
 if (c === '+') {
 	alert(a + b);
 };
  if (c === '-') {
 	alert(a - b);
 };
  if (c === '*') {
 	alert(a * b);
 };
  if (c === '/') {
 	alert(a / b);
 };